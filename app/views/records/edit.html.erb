<div class="container">
  <div class="center-block">
    <h1>タイム終了</h1>
      <%= form_with(model: @record, local: true) do |form| %>
        <div class="submit">
          <%= form.submit '開始' %>
        </div>

        <div class="submit">
          <%= form.submit '終了' %>
        </div>
      <% end %>


        開始時間 <%= Time.at(@record.starting_time) %><br>

  </div>
</div>